CREATE TABLE raw_analyst_ratings (id INT, headline VARCHAR, url VARCHAR, publisher VARCHAR, date TIMESTAMP, stock VARCHAR);
CREATE TABLE raw_partner_headlines (id INT, headline VARCHAR, url VARCHAR, publisher VARCHAR, date TIMESTAMP, stock VARCHAR);
