# start sql code
# output table name: sql_table

select
    `date` as date_1,
    `date` as HOOD_date_original_0,
    `close_last` as HOOD_close_last_original_2,
    `high` as HOOD_high_original_3,
    `low` as HOOD_low_original_4,
    `open` as HOOD_open_original_5
from
    `HOOD`
    ;