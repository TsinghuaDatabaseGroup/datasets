CREATE TABLE train
(
    TRIP_ID      BIGINT,
    CALL_TYPE    VARCHAR,
    ORIGIN_CALL  INT,
    ORIGIN_STAND INT,
    TAXI_ID      INT,
    TIMESTAMP    TIMESTAMP,
    DAY_TYPE     VARCHAR,
    MISSING_DATA VARCHAR,
    POLYLINE     VARCHAR
);
